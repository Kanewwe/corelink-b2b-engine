---
description: Commit and Push local changes to the UAT branch.
---
# /commit Workflow
Use this command after completing a feature or fix in your local environment.

1. **Commit to UAT**
// turbo
```powershell
./scripts/sync.ps1 -Action commit -Message "Your commit message"
```
