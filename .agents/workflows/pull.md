---
description: Pull latest code from GitHub to sync across devices.
---
# /pull Workflow
Use this command to keep your local `uat` branch in sync with GitHub when switching devices.

1. **Run Sync Command**
// turbo
```powershell
./scripts/sync.ps1 -Action pull
```
